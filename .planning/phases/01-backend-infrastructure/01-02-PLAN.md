---
phase: 01-backend-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - back/template.yaml
autonomous: true
gap_closure: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "template.yaml に Cognito Authorizer が定義されており、sam deploy で API Gateway に Authorizer が作成される"
    - "sam build および go build ./... がエラーなしで完了する"
    - "extractUserID が本番環境で Cognito JWT の sub クレームを正しく取得できる前提条件が整う"
  artifacts:
    - path: "back/template.yaml"
      provides: "Cognito Authorizer 定義 (CognitoAuthorizer) と CognitoUserPoolId Parameter"
      contains: "CognitoUserPoolId"
    - path: "back/template.yaml"
      provides: "API Gateway Globals に DefaultAuthorizer 設定"
      contains: "DefaultAuthorizer: CognitoAuthorizer"
  key_links:
    - from: "back/template.yaml (Globals.Api.Auth)"
      to: "Cognito User Pool us-east-1_LZhN7yrXj"
      via: "UserPoolArn: !Sub arn:aws:cognito-idp:${AWS::Region}:${AWS::AccountId}:userpool/${CognitoUserPoolId}"
      pattern: "CognitoUserPoolId"
---

<objective>
template.yaml に Cognito Authorizer を追加し、API Gateway が Cognito JWT を検証するよう設定する。

Purpose: VERIFICATION.md が検出したブロッカーを解消する。現状では template.yaml に Auth セクションがないため、sam deploy しても API は認証なしで公開される。extractUserID は常に "anonymous" を返し、異なるユーザーが同じトイレに報告しても全員が重複扱いされる。
Output: 修正された back/template.yaml — Cognito Authorizer が定義され、sam build が通る状態
</objective>

<execution_context>
@/Users/kimurahotaka/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimurahotaka/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@back/template.yaml
@back/.planning/phases/01-backend-infrastructure/01-VERIFICATION.md

<interfaces>
<!-- Cognito User Pool ID (既存リソース、別スタックで管理) -->
User Pool ID: us-east-1_LZhN7yrXj
User Pool ARN形式: arn:aws:cognito-idp:{region}:{accountId}:userpool/{userPoolId}

<!-- SAM Cognito Authorizer の正しい構文 (AWS::Serverless::Api) -->
Globals:
  Api:
    Auth:
      DefaultAuthorizer: CognitoAuthorizer
      Authorizers:
        CognitoAuthorizer:
          UserPoolArn: !Sub "arn:aws:cognito-idp:${AWS::Region}:${AWS::AccountId}:userpool/${CognitoUserPoolId}"

<!-- extractUserID が読む値 (server.go:265) -->
gatewayCtx.Authorizer["sub"]  // Cognito Authorizer が設定する JWT sub claim
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: template.yaml に Cognito Authorizer を追加する</name>
  <files>back/template.yaml</files>
  <read_first>
    - back/template.yaml (全体を読む — 現在の Globals, Parameters, Resources, Events.CatchAll を把握する)
    - back/.planning/phases/01-backend-infrastructure/01-VERIFICATION.md (gap の詳細を確認する)
  </read_first>
  <action>
以下の2箇所を back/template.yaml に追加・変更する。

**変更1: Parameters セクションに CognitoUserPoolId を追加**

既存の Parameters セクション (VapidPublicKey, VapidPrivateKey) に以下を追記する:

```yaml
  CognitoUserPoolId:
    Type: String
    Default: "us-east-1_LZhN7yrXj"
    Description: Cognito User Pool ID for API Gateway Authorizer
```

**変更2: Globals セクションに Api.Auth を追加**

既存の Globals.Function の下に、Globals.Api を追加する:

```yaml
  Api:
    Auth:
      DefaultAuthorizer: CognitoAuthorizer
      Authorizers:
        CognitoAuthorizer:
          UserPoolArn: !Sub "arn:aws:cognito-idp:${AWS::Region}:${AWS::AccountId}:userpool/${CognitoUserPoolId}"
```

**結果として Globals セクションは以下のようになる:**

```yaml
Globals:
  Function:
    Timeout: 30
    MemorySize: 256
    Runtime: provided.al2023
    Architectures:
      - arm64
  Api:
    Auth:
      DefaultAuthorizer: CognitoAuthorizer
      Authorizers:
        CognitoAuthorizer:
          UserPoolArn: !Sub "arn:aws:cognito-idp:${AWS::Region}:${AWS::AccountId}:userpool/${CognitoUserPoolId}"
```

注意: Events.CatchAll.Properties には Auth: セクションを個別追加しない。Globals.Api.Auth の DefaultAuthorizer が全エンドポイントに自動適用されるため。イベントレベルの上書きは不要。
  </action>
  <verify>
    <automated>grep -n "CognitoAuthorizer\|CognitoUserPoolId\|DefaultAuthorizer" /Users/kimurahotaka/Downloads/Neptune/back/template.yaml</automated>
  </verify>
  <done>
    - template.yaml に "CognitoUserPoolId" パラメータが存在する
    - template.yaml に "DefaultAuthorizer: CognitoAuthorizer" が存在する
    - template.yaml に "UserPoolArn: !Sub" が存在する
    - grep コマンドが3行以上の出力を返す
  </done>
</task>

<task type="auto">
  <name>Task 2: sam build でテンプレート構文を検証する</name>
  <files></files>
  <read_first>
    - back/template.yaml (変更後の内容を確認する)
  </read_first>
  <action>
以下のコマンドを順に実行し、template.yaml の構文とビルドが通ることを確認する。

```bash
cd /Users/kimurahotaka/Downloads/Neptune/back
go build ./...
```

go build が成功したら、sam validate で SAM テンプレート構文を確認する (sam CLI が使用可能な場合):

```bash
cd /Users/kimurahotaka/Downloads/Neptune/back
sam validate --template template.yaml --lint 2>/dev/null || echo "sam CLI not available, skipping sam validate"
```

sam validate が失敗した場合: エラーメッセージを読み、template.yaml の YAML インデント・キー名の誤りを修正する。

よくあるエラーと対処:
- "Invalid property" → SAM Globals.Api の Auth キー名を確認 (DefaultAuthorizer, Authorizers が正しいスペルか)
- "YAML parse error" → インデントが2スペースで統一されているか確認
- "UserPoolArn is required" → !Sub の ARN 文字列フォーマットを確認
  </action>
  <verify>
    <automated>cd /Users/kimurahotaka/Downloads/Neptune/back && go build ./...</automated>
  </verify>
  <done>
    - go build ./... がエラーなしで完了する (exit code 0)
    - template.yaml の CognitoAuthorizer 定義が構文的に正しい YAML である
  </done>
</task>

</tasks>

<verification>
両タスク完了後に以下を確認する:

1. grep で必須キーが存在することを確認:
   ```bash
   grep -c "CognitoAuthorizer\|CognitoUserPoolId\|DefaultAuthorizer" /Users/kimurahotaka/Downloads/Neptune/back/template.yaml
   ```
   出力が 3 以上であること。

2. go build が通ること:
   ```bash
   cd /Users/kimurahotaka/Downloads/Neptune/back && go build ./...
   ```

3. template.yaml に Auth セクションが Globals レベルに存在することを確認:
   ```bash
   grep -A 6 "^  Api:" /Users/kimurahotaka/Downloads/Neptune/back/template.yaml
   ```
</verification>

<success_criteria>
- back/template.yaml に Cognito Authorizer が定義されている (DefaultAuthorizer: CognitoAuthorizer)
- CognitoUserPoolId パラメータが "us-east-1_LZhN7yrXj" をデフォルト値として持つ
- go build ./... がエラーなしで完了する
- VERIFICATION.md のギャップ「template.yaml に Cognito Authorizer が定義されていない」が解消される
- sam deploy 後は API Gateway が Cognito JWT を検証し、extractUserID が sub クレームを正しく取得できる
</success_criteria>

<output>
完了後、以下のサマリーファイルを作成する:
.planning/phases/01-backend-infrastructure/01-02-SUMMARY.md

内容:
- 変更したファイル: back/template.yaml
- 追加した内容: Globals.Api.Auth (DefaultAuthorizer: CognitoAuthorizer), Parameters.CognitoUserPoolId
- 閉じたギャップ: VERIFICATION.md の "Cognito Authorizer が SAM template に存在しない" (Success Criterion #4)
- 検証結果: go build ./... pass
</output>
